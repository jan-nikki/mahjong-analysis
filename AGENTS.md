# Project Overview

天鳳鳳凰卓の牌譜を用いて、麻雀戦術を統計分析するPythonプロジェクト。

## Analysis Policy

- 四人麻雀の鳳凰卓東南戦を対象とする
- 基本的に東場のみを分析対象とする
- 南場は順位条件によって打牌方針が変化するため除外する
- 主な分析期間は2020年〜2025年とする
- 年別の結果も確認し、年代差を検証する
- 元データはMJAI形式を使用する

## Development

- Python 3.12を使用する
- テストにはpytestを使用する
- 麻雀ルールの判定ロジックと統計集計処理を分離する
- 共通処理はsrc/mahjong_analysis/に置く
- 分析ごとの実行コードはanalysis/に置く
- 分析仕様はdocs/specs/に置く
- 新しい重要な判定ロジックにはテストを作成する
- 実装後はpytestを実行する

## Validation

- AIが生成したコードを無条件に信用しない
- 人工テストケースで検証する
- 実牌譜の局IDからMJAI原本を目視確認できるようにする
- 検出したケースだけでなく、取りこぼしの可能性も検証する
- 可能な場合は既存研究や独立実装と比較する

## Model Workflow

- 通常の仕様作成、実装、実行、結果整理にはSolの極高（`xhigh`）を使用する
- コードレビューと本番実行後の研究監査にはAstraの極高（`xhigh`）を使用する
- AstraのUltra（`ultra`）は、極高のレビュー後も重大な問題や解決しない論点が残る場合に限る
- レビューや監査の報告では、「Astraでレビュー」のようにモデル名だけを書かず、モデル名と推論強度（例: `Astra / xhigh`）を明記する
- 推論強度を確認できない実行は、強度を推測せず「未確認」と報告する

## Visual Assets

- note記事などのサムネイルを画像生成AIで作る場合、麻雀牌を描かせない
- 画像生成AIでは牌面、牌種、牌の枚数が崩れやすいため、麻雀らしさは卓色、河を連想させる抽象グリッド、統計グラフ、数式、比較構図、タイポグラフィで表現する
- 正確な牌姿や牌面が必要な図は、画像生成AIではなく、検証済みの牌画像素材またはコードによる決定的な描画で作成する

## Data

- data/raw/ は元MJAIデータを保存する
- data/processed/ はParquet等の加工済みデータを保存する
- 元データや大量の生成データはGitHubにcommitしない
